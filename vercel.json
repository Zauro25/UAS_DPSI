{
    "version": 2,
    "builds": [
    {
        "src": "./app.js",
        "use": "@vercel/node"
    }
],
"routes": [
    {
        "src": "/(.*)",
        "dest": "/"
    }
]
}
{
  "scripts": {
    "build": "npm install && npm run build"
  }
}
